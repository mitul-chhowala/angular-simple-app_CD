name: Build and Deploy Angular App

on:
  push:
    branches:
      - master  # Ensure this matches your branch name (master or main)

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        # Critical for Angular 5 dependencies on Node 20
        run: npm install --legacy-peer-deps

      - name: Build Project
        # Correct PowerShell syntax for Windows runners
        run: |
          $env:NODE_OPTIONS = "--openssl-legacy-provider"
          npm run build --prod

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: dist/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'angular-simple-app-Mitul-2026'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_9A62 }}
